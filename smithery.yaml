# Smithery configuration file: https://smithery.ai/docs/config#smitheryyaml

startCommand:
  type: stdio

commandFunction:
  |-
  (config) => ({
    command: 'node',
    args: ['build/mcp/index.js']
  })
